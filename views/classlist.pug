html  
  head
    title= title
    link(rel='stylesheet', href='./style.css')
  body
    h1= "RolzPro AAW"
    h2= "Full ClassList for RolzPro AAW Database:"
    h4= "If ClassList does not show up, try reloading."
    textarea(rows="20", cols="50", name='listbox')=listmessage
    
    form(id="back", method="GET", class="form"  action="/")
        br
        button#submitButton(type='submit') BACK TO MAIN PAGE